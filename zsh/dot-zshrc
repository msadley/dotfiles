autoload -Uz glob

# Load all .zsh modules
for file in /home/adley/.config/zsh/**/*.zsh(.N); do
  source "$file"
done

# Automatically start hyprland from tty
if [ -z "$DISPLAY" ] && [ "$(tty)" = "/dev/tty1" ]; then
      exec start-hyprland
fi
