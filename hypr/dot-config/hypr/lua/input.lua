return function()
  hl.config({
    input = {
      kb_layout = "br, us",
      kb_variant = "",
      kb_options = "grp:alt_shift_toggle",

      follow_mouse = 1,
      sensitivity = 0,

      touchpad = {
        natural_scroll = true,
      }
    }
  })
end
